
// Build Options
APPLICATION_EXTENSION_API_ONLY = YES

// Deployment
SKIP_INSTALL = YES

// Linking
DYLIB_INSTALL_NAME_BASE = @rpath

// Packaging
INFOPLIST_FILE = WireCommonComponents/Info.plist
PRODUCT_NAME = $(TARGET_NAME)
PRODUCT_BUNDLE_IDENTIFIER = $(WIRE_BUNDLE_ID).SecretCommonComponents
